/* tslint:disable */

import {
  Inject,
  Injectable,
  Optional
} from '@angular/core';
import {
  API_BASE_URL,
  API_WITH_CREDENTIALS
} from './variables';

/**
 * Services configuration
 */
@Injectable({
  providedIn: 'root'
})
export class {{name}} {

  public baseUrl: string = '{{{baseUrl}}}';

  public withCredentials: boolean = false;
  {{#apiKeys}}

  public readonly apiKeys: Map<string, string> = new Map();
  {{/apiKeys}}
  {{#accessTokens}}

  public readonly accessTokens: Map<string, string> = new Map();
  {{/accessTokens}}

  constructor(
    @Optional() @Inject(API_BASE_URL) baseUrl: string,
    @Optional() @Inject(API_WITH_CREDENTIALS) withCredentials: boolean
  ) {
    if (baseUrl != null) {
      this.baseUrl = baseUrl;
    }

    this.withCredentials = !!withCredentials;
  }
}
